name: Test

on:
  push:
    branches-ignore:
      - master
      - openapi
  pull_request_target:

jobs:
  test:
    uses: clambin/workflows/.github/workflows/test.yaml@go1.23
    secrets:
      codecov-token: ${{ secrets.CODECOV_TOKEN }}
